{
  "name": "date-operations-mcp",
  "version": "1.0.0",
  "description": "MCP server for reliable date calculations with UK context",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "date", "calendar", "uk"],
  "author": "James Murrell",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "date-fns": "^2.30.0",
    "date-fns-tz": "^2.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}